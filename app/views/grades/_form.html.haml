= simple_form_for(@grade) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    =f.input :description, :as => :text, :required => true, :input_html => {:cols => 20, :rows => 5}

  .form-actions
    = f.button :submit
