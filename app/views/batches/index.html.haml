%h1 Showing batches

%table
  %tr
    %th Batch Title
    %th Grade Title
    %th Starte Date
    %th End Time
    %th Days of week
    %th
    %th
    %th

  - @batches.each do |batch|
    %tr
      %td= batch.title
      %td= batch.grade.title
      %td= batch.starte_date
      %td= batch.end_date
      %td= batch.from_time
      %td= batch.end_time
      %td= batch.deys_of_week
      %td= link_to 'Show', batch
      %td= link_to 'Edit', edit_batch_path(batch)
      %td= link_to 'Delete', batch, :method => :delete, :data => { :confirm => 'Are you sure? if you select delete option, your your batch will be delete. What do you want to do?' }

%br

= link_to 'New Batch', new_batch_path
