

%h1 Students list
= simple_form_for destroy_multiple_students_path, method: :delete  do 
  = render "students_list"
  = submit_tag "delete selected"
  = link_to '<button type ="button">New Student</button>'.html_safe, new_student_path
 