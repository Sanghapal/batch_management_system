%h1 Listing trainers

%table
  %tr
    %th
    %th
    %th

  - @trainers.each do |trainer|
    %tr
      %td= link_to 'Show', trainer
      %td= link_to 'Edit', edit_trainer_path(trainer)
      %td= link_to 'Destroy', trainer, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'Create Trainer', new_trainer_path
